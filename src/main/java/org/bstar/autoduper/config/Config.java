package org.bstar.autoduper.config;

public class Config {
    public double inventoryDelay = 0.5; // in seconds
    public double keyPressDelay = 0.2; // in seconds
    public boolean debugMode = false;
}